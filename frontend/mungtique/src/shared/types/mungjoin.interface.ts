export interface MyMung {
  myMungId?: number;
  image?: { url: string };

  mungName: string;
  breed: string;
  weight: number;
  age: number;
  gender: string;
  fixed: string;
  userId: number;
}
